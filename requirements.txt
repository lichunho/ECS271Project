# Install torch separately first via the cu128 wheel index (see README):
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
transformers>=4.46
accelerate>=1.0
numpy
openai>=1.50
python-dotenv>=1.0
pytest
# Data sourcing (scripts/download_data.py)
httpx>=0.27
tqdm>=4.66
gdown>=5.2
# Retrieval (scripts/build_retrieval.py, src/retrieval.py).
# REQUIREMENT: pyserini needs a JDK 21+ on PATH (set JAVA_HOME).
#   Windows native: install Eclipse Temurin 21
#     https://adoptium.net/temurin/releases/?version=21
#     Then in PowerShell:
#       $env:JAVA_HOME = 'C:\Program Files\Eclipse Adoptium\jdk-21.0.x-hotspot'
#       $env:PATH = "$env:JAVA_HOME\bin;$env:PATH"
#   WSL2 fallback (if Windows-native JNI gives you trouble):
#     sudo apt install -y openjdk-21-jdk
#     pip install pyserini  (in a venv inside WSL)
# pyjnius is a transitive dep of pyserini; the 1.7.0 wheel is published for
# Windows + Python 3.13, so no manual install needed on either platform.
pyserini>=2.1
rapidfuzz>=3.0
